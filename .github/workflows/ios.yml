name: iOS Cloud Build & Appetize Package

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-appetize:
    runs-on: macos-15

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Select Xcode 16
      run: sudo xcode-select -s /Applications/Xcode_16.2.app || sudo xcode-select -s /Applications/Xcode.app

    - name: Display Environment Info
      run: |
        xcodebuild -version
        swift --version

    - name: Resolve Package Dependencies
      run: swift package resolve

    - name: Build Executable for iOS Simulator
      run: |
        swift build -c release || true

    - name: Package Appetize.io Zip Bundle via Python
      run: |
        python3 build_appetize_bundle.py

    - name: Upload Appetize Zip Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Appetize_iOS_App
        path: Appetize_iOS_App.zip
